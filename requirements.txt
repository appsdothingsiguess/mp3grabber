# MP3 Grabber Python Dependencies

# Core Dependencies
faster-whisper==1.0.3     # High-performance Whisper implementation (pinned for stability)
yt-dlp>=2023.0.0          # Stream downloader with HLS/DASH support
numpy>=1.20.0             # Required for faster-whisper and GPU testing
requests>=2.28.0          # Required by faster-whisper (model download, utils)

# GPU Acceleration (NVIDIA only - CUDA 12)
# These libraries are required for GPU acceleration with CUDA 12
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*
